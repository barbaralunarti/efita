# ⚠️  IMPORTANTE: Copie este arquivo para .env e preencha com seus valores!
# Nunca commite .env ou valores sensíveis no Git

# SMTP Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=efita@ita.br
SMTP_PASSWORD=app-password-aqui
SMTP_FROM="EFITA Coordenação <efita@ita.br>"

# App Configuration
PIX_CODE=00020126...
AGENDA_URL=https://efita.ita.br/agenda

# JWT Security (⚠️  CRÍTICO: Gere uma chave segura!)
# Gerar com: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=sua-chave-secreta-aleatoria-aqui-32-caracteres

# Frontend Configuration
FRONTEND_ORIGIN=http://localhost:5173
